[user]
	name = Laoujin
	email = woutervs@hotmail.com

[alias]
	# Extremely common aliases
	ci = commit
	br = branch
	co = checkout
	df = diff

	# Find more useful aliases all over the interwebs!

[credential]
	helper = !'C:\\Users\\Wouter\\AppData\\Roaming\\GitCredStore\\git-credential-winstore.exe'

# Available colors: normal, black, red, green, yellow, blue, magenta, cyan, or white
# Second optional color param values: bold, dim, ul (underline), blink, and reverse (swap foreground and background)
# Posh-Git colors are defined in MYDOCUMENTS\WindowsPowerShell profile!

[color "branch"]
	current = yellow
	local = white
	remote = magenta

[color "status"]
	header = white
	changed = magenta bold
	untracked = magenta bold
	unmerged = magenta reverse
	added = yellow
	branch = white

[color "diff"]
	meta = blue white
	frag = normal bold
	old = magenta bold
	new = green bold