9u30 - 10u30: Git for .NET devs



https://presentate.com/bobthecow/talks/changing-history



git update-index --assume-unchanged filename


make sure no keys, passwords are in any commit when pushing to github
--> Amazon keys are being harvested on GitHub and used for bitcoin mining

pull without merge commit
http://stackoverflow.com/questions/11575398/how-can-i-save-my-secret-keys-and-password-securely-in-my-version-control-system
24 april

https://github.com/tj/git-extras/wiki/Commands#git-extras

git pull 
vs
git fetch
git merge

--> include reg files voor git bash / powershell / st3

git ls-files --> uses .gitignore, 
git grep "html" -- "*.js"
git log --grep=fake
git log --grep=frotz --and --author=Linus
git grep -l --all-match -e shutdown -e localhost

Configure mergetool
Configure editor (vi)





Git – zou eerst kunnen checken wat de Git ervaring is en/of welke interesses om dan ergens een selectie te doen uit:
•         Basics
o   Commando’s: init, add, status, commit, log, diff, push, pull, merge
o   Git internals: den DAG ?
o   Git in Visual Studio (en hoe snel je tegen de beperkingen van hun implementatie aanloopt: vb geen meerdere remotes mogelijk)
o   SeeGit (tooltje van de Haack dat wel helpt om alles wat te visualiseren)
o   Git flow
o   Commando’s: stash, branching, checkout, amending, cherry-pick, rebase, revert, …
o   Git modules



Get all occurences of console.log() in working directory / index

git config --global fetch.prune true
git fetch -p




Verzamelde mails:
Basics:
git init
-	Creates a .git directory – and that’s it!


Git commit
Git commit --amend
?	Oeps!
Git reset --hard HEAD@{1}
?	Check stackoverflow for details




git add --interactive

https://help.github.com/articles/remove-sensitive-data/
more stuff with update-index –assume-unchanged





git reset --hard origin/master
?	Remove local merge commits…

git diff --stat origin/master
git log origin/master..master


http://justinhileman.info/article/git-pretty/git-pretty.png


Git clean -f // clear untracked files
Git clean -f --dry-run
Git clean -f -d // also delete dirs.
Git clean -f -X // delete gitignored files
Git clean -i  // interactive


“Both Modified”
git mergetool --tool=bc3

git mergetool –y // don’t ask for each file

git checkout --ours filename.c
git checkout --theirs filename.c
git add filename.c
git commit -m "using theirs"

<<<<<<<
Changes made on the branch that is being merged into. In most cases,
this is the branch that I have currently checked out (i.e. HEAD).
|||||||
The common ancestor version.
=======
Changes made on the branch that is being merged in. This is often a 
feature/topic branch.
>>>>>>>


# Common base version of the file.
git show :1:some_file.cpp

# 'Ours' version of the file.
git show :2:some_file.cpp

# 'Theirs' version of the file.
git show :3:some_file.cpp


http://stackoverflow.com/questions/1459150/how-to-undo-git-commit-amend-done-instead-of-git-commit


Under [branch "master"], try adding the following to the repo's Git config file (.git/config):
[branch "master"]
    remote = origin
    merge = refs/heads/master


git br -u origin/sass
?	Track remote branch

gitconfig is awesome: "[push] default=tracking

git branch --set-upstream-to=upstream/foo localfoo


